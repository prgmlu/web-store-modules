const BackgroundFaces = Object.freeze({
    LEFT: 0, // px, left
    RIGHT: 1, // nx, right
    TOP: 2, // py, top
    BOTTOM: 3, // ny, bottom
    BACK: 4, // pz, front
    FRONT: 5, // nz, back
});

export default BackgroundFaces;
